import 'package:flutter/material.dart';

class PlanView extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("事务计划"),centerTitle: true,),
    );
  }
}